# Model 1
server {
    listen 8001;
    listen [::]:8001;

    server_name _;
    server_tokens off;
    client_max_body_size 50m;

    location /v1/ {
        proxy_pass http://192.168.10.21:8000;
        proxy_http_version 1.1;

        # Remove PyCharm/Ktor h2c headers.
        proxy_set_header Connection "";
        proxy_set_header Upgrade "";
        proxy_set_header HTTP2-Settings "";

        proxy_set_header Authorization $http_authorization;
        proxy_set_header Host $proxy_host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;

        proxy_request_buffering on;

        # Required for streamed AI responses.
        proxy_buffering off;
        proxy_cache off;
        add_header X-Accel-Buffering "no" always;

        proxy_connect_timeout 30s;
        proxy_send_timeout 3600s;
        proxy_read_timeout 3600s;
    }

    location = /health {
        proxy_pass http://192.168.10.21:8000/health;
        proxy_http_version 1.1;

        proxy_set_header Connection "";
        proxy_set_header Upgrade "";
        proxy_set_header HTTP2-Settings "";
    }

    location / {
        return 404;
    }
}

# Model 2
server {
    listen 8002;
    listen [::]:8002;

    server_name _;
    server_tokens off;
    client_max_body_size 50m;

    location /v1/ {
        proxy_pass http://192.168.10.22:8000;
        proxy_http_version 1.1;

        # Remove PyCharm/Ktor h2c headers.
        proxy_set_header Connection "";
        proxy_set_header Upgrade "";
        proxy_set_header HTTP2-Settings "";

        proxy_set_header Authorization $http_authorization;
        proxy_set_header Host $proxy_host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;

        proxy_request_buffering on;

        # Required for streamed AI responses.
        proxy_buffering off;
        proxy_cache off;
        add_header X-Accel-Buffering "no" always;

        proxy_connect_timeout 30s;
        proxy_send_timeout 3600s;
        proxy_read_timeout 3600s;
    }

    location = /health {
        proxy_pass http://192.168.10.22:8000/health;
        proxy_http_version 1.1;

        proxy_set_header Connection "";
        proxy_set_header Upgrade "";
        proxy_set_header HTTP2-Settings "";
    }

    location / {
        return 404;
    }
}
